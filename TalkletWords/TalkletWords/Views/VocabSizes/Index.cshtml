@model IEnumerable<TalkletWords.Models.VocabSize>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Age)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Vocabulary)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vocabulary)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VocabSizeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VocabSizeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VocabSizeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
